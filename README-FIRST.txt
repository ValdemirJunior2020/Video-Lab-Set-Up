MoneyPrinterTurbo Ollama Director V8 — EXPRESSIVE NARRATION

Run INSTALL-AND-TEST.bat

Keeps Ollama qwen3:8b + Pexels + subtitles + FFmpeg.
Adds Chatterbox expressive narration in an isolated CPU-first Docker service.
Fallback: Chatterbox -> Piper -> Edge.
Microsoft Playwright remains mandatory.

APPROVED is printed only after syntax, Docker compile, Ollama, Pexels, real Chatterbox synthesis, Piper fallback, WebUI, and Playwright all pass.

Proof files after pass:
MoneyPrinterTurbo\V8_CHATTERBOX_TEST.wav
MoneyPrinterTurbo\V8_PLAYWRIGHT_PASS.png
